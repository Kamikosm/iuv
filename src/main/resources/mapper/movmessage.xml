<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iuv.dao.MovmessageDao">

	<!-- 首页轮播图跳转电影详情 -->
	<select id="findLbMsg" resultType="com.iuv.pojo.movie.Movie">
		select id,mv_name mvName,type,mv_time mvTime,start_time startTime,message,score,sum,
		big_pic bigPic from movie where mv_Name=#{mvName}
	</select>

</mapper>